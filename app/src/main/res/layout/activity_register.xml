<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_register"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:theme="@style/Lost2FoundTheme"
    tools:context="es.lost2found.loginregisterUI.LoginActivity">

    <LinearLayout
        android:id="@+id/register_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:orientation="vertical">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="30dp"
        android:layout_marginStart="30dp"
        android:layout_marginTop="60dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_logo" />

    <EditText
        android:id="@+id/email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/email_textloginregister"
        android:inputType="textEmailAddress"
        android:paddingTop="25dp"
        android:textSize="17sp" />

    <EditText
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/name_textregister"
        android:inputType="text"
        android:paddingTop="20dp"
        android:textSize="17sp" />

    <EditText
        android:id="@+id/password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/pass_textloginregister"
        android:inputType="textPassword"
        android:paddingTop="20dp"
        android:textSize="17sp" />

    <EditText
        android:id="@+id/repassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/passagain_textregister"
        android:inputType="textPassword"
        android:paddingTop="20dp"
        android:textSize="17sp" />

    <Button
        android:id="@+id/register_button"
        android:layout_marginTop="20dp"
        android:text="@string/register_button_text"
        style="@style/buttonStyle" />
    <!--android:onClick="register"-->

    <TextView
        android:id="@+id/user_already_exists"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingTop="10dp"
        android:textAlignment="center"
        android:textColor="@color/colorPrimary" />

    <TextView
        android:id="@+id/login_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="@string/alreadyhaveaccount"
        android:textAlignment="center"
        android:textColor="@color/textOccasionalColor" />

    <Button
        android:id="@+id/login_button"
        android:layout_marginTop="15dp"
        android:text="@string/login_button_text"
        android:onClick="login"
        style="@style/buttonStyle" />

    </LinearLayout>

</android.support.constraint.ConstraintLayout>