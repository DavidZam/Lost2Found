<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_place"
    android:layout_width="wrap_content"
    android:layout_height="match_parent">

    <RelativeLayout android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".lost2foundUI.placeUI.transportUI.TransportPlaceActivity"
        tools:showIn="@layout/activity_place"
        android:background="@color/colorSecondaryText">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tipos de lugar"
            android:theme="@style/ToolbarStyle">

        </android.support.v7.widget.Toolbar>

        <RelativeLayout
            android:id="@+id/login_layout"
            android:layout_width="match_parent"
            android:padding="16dp"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:layout_below="@+id/toolbar_center"
            android:orientation="vertical">

            <Button
                android:id="@+id/metro_transport_place_button"
                style="@style/buttonStyle"
                android:layout_above="@+id/bus_transport_place_button"
                android:layout_alignParentStart="true"
                android:layout_marginBottom="63dp"
                android:onClick="fillTransportPlace"
                android:text="@string/underground_transport_button_text" />
            <!--android:onClick="place"-->

            <Button
                android:id="@+id/bus_transport_place_button"
                style="@style/buttonStyle"
                android:layout_alignParentBottom="true"
                android:layout_alignParentStart="true"
                android:layout_marginBottom="195dp"
                android:onClick="fillTransportPlace"
                android:text="@string/bus_transport_button_text" />
            <!--android:onClick="place"-->

            <!--<Button
                android:id="@+id/airport_transport_place_button"
                style="@style/buttonStyle"
                android:layout_alignParentStart="true"
                android:layout_below="@+id/bus_transport_place_button"
                android:layout_marginTop="76dp"
                android:text="@string/airport_transport_button_text" />-->

        </RelativeLayout>

    </RelativeLayout>


</android.support.constraint.ConstraintLayout>